<Window x:Class="_05_async_await_ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_05_async_await_ui"
        mc:Ignorable="d"
        Title="Main Window" Height="450" Width="800">
    <DockPanel Margin="10">
        <!--<DockPanel DockPanel.Dock="Top">
            <Label VerticalContentAlignment="Center">Directory:</Label>
            <Button Click="Button_Click_1" MinWidth="100" DockPanel.Dock="Right" VerticalAlignment="Center">Start</Button>
            <Button Click="Button_Click" MinWidth="100" Margin="10,0" DockPanel.Dock="Right" VerticalAlignment="Center">Open</Button>
            <TextBox Text="{Binding SelectedDirPath}" VerticalAlignment="Center"></TextBox>
        </DockPanel>-->
        <Button Click="Button_Click" DockPanel.Dock="Top">Start</Button>
        <Button Click="Button_Click_1" DockPanel.Dock="Top">Reset</Button>
        <ProgressBar Value="{Binding Progress}" DockPanel.Dock="Bottom" Name="progressBar" Margin="0,10" Height="20"></ProgressBar>
        <!--<ListBox x:Name="list"></ListBox>-->
    </DockPanel>
</Window>
